# ==========================================================================
# astrbot_plugin_lingxi —— .gitignore
#
# 仓库根 = 插件目录本身（main.py / metadata.yaml / core/ / utils/ 直接摊在根下）。
# 打包脚本 build_plugin.py 会把这个仓库塞进一层 astrbot_plugin_lingxi/ 再压成 zip，
# 所以这里忽略的内容不会被带进发行包。
# ==========================================================================

# --------------------------------------------------------------------------
# Python 字节码与缓存
# --------------------------------------------------------------------------
__pycache__/
*.py[codz]
*$py.class
*.so

# --------------------------------------------------------------------------
# 虚拟环境
# --------------------------------------------------------------------------
.venv
venv/
env/
ENV/
env.bak/
venv.bak/

# --------------------------------------------------------------------------
# 构建产物
# 注意：dist/ 是 build_plugin.py 的输出目录，绝不该入库
# --------------------------------------------------------------------------
build/
dist/
*.egg-info/
*.egg
wheels/
sdist/
develop-eggs/
.eggs/

# --------------------------------------------------------------------------
# 工具缓存
# --------------------------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.cache/
.pyright/

# --------------------------------------------------------------------------
# 本地手工备份（本项目特有）
#
# 开发期长期靠「复制一份再改」的方式改动，测试端目录里曾积下 16 个这类文件，
# 形如：
#     main.py.bak-20260921
#     core/group_chime.py.bak-20260921b
#     _conf_schema.json.bak-before-followup
#     core/web_admin_server.py.removed
# 这些是历史垃圾，一律不入库。凡是要保留的版本，请走 git 提交。
# --------------------------------------------------------------------------
*.bak
*.bak-*
*.bak_*
*.orig
*.rej
*.removed
*.old

# --------------------------------------------------------------------------
# 编辑器 / IDE
# --------------------------------------------------------------------------
.idea/
.vscode/
.devin/
*.swp
*.swo
*~

# --------------------------------------------------------------------------
# 操作系统
# --------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# --------------------------------------------------------------------------
# 运行时数据
#
# 按 AstrBot 规范，插件数据必须落在
#     data/plugin_data/astrbot_plugin_lingxi/
# 而非插件自身目录。这里加上保险，防止有人图省事把数据写进仓库。
# --------------------------------------------------------------------------
session_data.json
plugin_data/
*.log
logs/

# --------------------------------------------------------------------------
# 发行包
# 压缩包是构建产物，由 build_plugin.py 生成，不入库。
# --------------------------------------------------------------------------
*.zip
